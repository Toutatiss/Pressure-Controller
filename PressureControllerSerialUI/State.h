// Enum definition in a separate header file (Pump.h)
#ifndef STATE_H
#define STATE_H

enum State
{
  STATE_DIFFERENTIAL,
  STATE_EXPERIMENT,
  STATE_RESET,
  STATE_SET_PARAMETERS,
  STATE_FUNCTION,
  STATE_HELP
};

#endif
